{% extends 'admin/base.html.twig' %}

{% block admin_title %}Nouvelle langue{% endblock %}

{% block admin_body %}
    <div class="card shadow-sm">
        <div class="card-header">
            <h2 class="h5 mb-0">Ajouter une langue</h2>
        </div>
        <div class="card-body">
            {{ include('bibliothecaire/langue/_form.html.twig') }}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('app_langue_index') }}" class="btn btn-secondary btn-sm">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
