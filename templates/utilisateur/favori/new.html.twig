{% extends 'utilisateur/base.html.twig' %}

{% block title %}Nouveau favori{% endblock %}

{% block content %}
    <div class="card shadow-sm">
        <div class="card-header">
            <h2 class="h5 mb-0">Ajouter un favori</h2>
        </div>
        <div class="card-body">
            {{ include('utilisateur/favori/_form.html.twig') }}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('app_favori_index') }}" class="btn btn-secondary btn-sm">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
