{% extends 'utilisateur/base.html.twig' %}

{% block title %}Nouvelle réservation{% endblock %}

{% block content %}
    <div class="card shadow-sm">
        <div class="card-header">
            <h2 class="h5 mb-0">Créer une réservation</h2>
        </div>
        <div class="card-body">
            {{ include('utilisateur/reservation/_form.html.twig') }}
        </div>
        <div class="card-footer text-end">
            <a href="{{ path('app_reservation_index') }}" class="btn btn-secondary btn-sm">Retour aux réservations</a>
        </div>
    </div>
{% endblock %}
